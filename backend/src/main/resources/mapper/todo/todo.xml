<?xml version="1.0" encoding="UTF-8"?>

<!--MyBatisを使用するxmlファイルであることを宣言 -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.example.wiki.todo.mapper.TodoMapper">

    <!-- Todo一覧表示 - 全件検索 -->
    <select id="getAllTodoList" resultType="com.example.wiki.todo.dto.TodoDto">
        SELECT 
            t.id
            ,t.title
            ,t.priority
            ,t.status
        FROM app.todos t
        ORDER BY id DESC
    </select>
    
    <!-- Todo1件登録 -->
    <insert id="insertTodo">
        INSERT INTO app.todos (title, priority, status) 
        VALUES (#{title}, "1", #{status}) RETURNING id
    </insert>
    
    <!-- Todo更新（完了↔未完了） -->
    <update id="updateTodo" parameterType="com.example.wiki.todo.dto.TodoDto">
        UPDATE app.todos
            SET title = #{title}, status = #{status}
        WHERE id = #{id}
    </update>

    <!-- Todo優先度更新 -->
    <update id="updatePriorities" parameterType="java.util.List">
      <foreach collection="todoDto" item="todo" separator=";">
        UPDATE app.todos
        SET priority = #{todo.priority}
        WHERE id = #{todo.id}
      </foreach>
    </update>

</mapper>